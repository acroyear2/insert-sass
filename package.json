{
  "name": "insert-sass",
  "version": "1.0.0",
  "description": "sass compilation / css insertion transform",
  "main": "index.js",
  "dependencies": {
    "css-loader": "^0.28.0",
    "insert-css": "^2.0.0",
    "node-sass": "^4.5.2",
    "through2": "^2.0.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "browserify": "^14.3.0",
    "jsdom": "9.x",
    "tape": "^4.6.3"
  }
}
