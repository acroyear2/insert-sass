{
  "name": "@xx55/insert-sass",
  "version": "0.1.1",
  "description": "sass compilation / css insertion transform",
  "main": "index.js",
  "dependencies": {
    "css-loader": "^0.28.0",
    "node-sass": "^4.5.2",
    "through2": "^2.0.3",
    "insert-css": "^2.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
